# doc

`flipped_upqc_pcim.tex` / `flipped_upqc_pcim.pdf` — *A Flipped Unified Power Quality
Conditioner for Active Distribution Networks: Topology, Modelling, and Control Strategy*,
D. Bagnara, IEEE conference format (`IEEEtran`), prepared for PCIM.

Sections: Introduction; F-UPQC Topology (classical vs. flipped UPQC, component
description); Mathematical Model (reference frame, shunt VSC, series VSC, DC link);
Control Strategy (shunt inner current loop, DC-bus voltage outer loop, series voltage
compensation loop); Steady-State Analysis and Component Rating (power flow, converter
rating); Regulatory Alignment; Conclusion.

Build with `pdflatex` (two passes); the bibliography is embedded in the source.
